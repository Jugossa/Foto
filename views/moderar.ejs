<!DOCTYPE html>
<html>
<head>
  <title>Moderación de Fotos</title>
  <style>
    body { font-family: sans-serif; padding: 20px; background: #f0f0f0; }
    h1 { text-align: center; }
    .grid { display: flex; flex-wrap: wrap; gap: 20px; justify-content: center; }
    .foto {
      background: white;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 10px;
      width: 200px;
      text-align: center;
      box-shadow: 2px 2px 10px rgba(0,0,0,0.1);
    }
    .foto img {
      width: 100%;
      border-radius: 6px;
      margin-bottom: 10px;
    }
    .foto button {
      margin: 5px;
      padding: 5px 10px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-weight: bold;
    }
    .approve { background-color: #4CAF50; color: white; }
    .reject { background-color: #f44336; color: white; }
  </style>
</head>
<body>
  <h1>📤 Panel de Moderación</h1>
  <div class="grid">
    <% fotos.forEach(foto => { %>
      <div class="foto">
        <img src="<%= foto.thumbnailLink %>" alt="<%= foto.name %>">
        <p><%= foto.name %></p>
        <a href="/approve/<%= foto.id %>"><button class="approve">✅ Aprobar</button></a>
        <a href="/reject/<%= foto.id %>"><button class="reject">❌ Rechazar</button></a>
      </div>
    <% }) %>
  </div>
</body>
</html>
