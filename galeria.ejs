
<!DOCTYPE html>
<html>
<head>
  <title>Galería del Evento 📸</title>
  <style>
    body {
      margin: 0;
      background: black;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      overflow: hidden;
    }
    img {
      max-width: 100%;
      max-height: 100%;
      object-fit: contain;
      border-radius: 20px;
      box-shadow: 0 0 20px rgba(255,255,255,0.3);
    }
  </style>
</head>
<body>
  <img id="slideshow" src="" alt="Galería">
  <script>
    const fotos = <%- JSON.stringify(fotos) %>;
    let index = 0;

    function showNext() {
      if (fotos.length === 0) return;
      document.getElementById('slideshow').src = fotos[index].thumbnailLink.replace("=s220", "=s1600");
      index = (index + 1) % fotos.length;
    }

    setInterval(showNext, 4000);
    showNext();
  </script>
</body>
</html>
